// Intentionally empty for FT-AUTH-002. A later reviewed action must add an
// exact docs/research Markdown heading reference before any record can move
// to human-reviewed, cleared-data, or verified-source state.
export const approvedEvidenceReferences = Object.freeze([]);
